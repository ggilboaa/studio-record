# אפיון: Studio Record — מצלמה וירטואלית והקלטה למק

**שם האפליקציה:** Studio Record
**פלטפורמה:** macOS (Apple Silicon + Intel), Swift + SwiftUI
**מכונת פיתוח:** מק מיני M4. התוצר הסופי מותקן על כל המקים.
**עדכון אחרון:** יולי 2026

---

## 1. תמצית

אפליקציית מק המשדרת סצנה מורכבת כ**מצלמה וירטואלית** לזום (ולכל אפליקציית וידאו), עם **הקלטה מקומית** מקבילה הכוללת וידאו + שני ערוצי אודיו. שלוש סצנות, עיגול מצלמה צף, ממשק בעברית.

**עקרון הפיצול:** המצלמה הווירטואלית מעבירה וידאו בלבד (בזום בוחרים את המיקרופון הרגיל). האודיו — מיקרופון + סאונד מערכת — נלכד רק להקלטה המקומית. אין התקני אודיו וירטואליים ואין דרייברים.

---

## 2. שלוש הסצנות

### סצנה 1 — מצלמה בלבד
- וידאו: המצלמה הנבחרת במסך מלא.
- שימוש: שיחת זום רגילה עם שליטה מלאה (flip, בחירת מצלמה).

### סצנה 2 — חלון + עיגול צף
- וידאו: לכידת חלון נבחר (ScreenCaptureKit) — בעיקר חלון שיקוף ה-iPad — ומעליו עיגול מצלמה צף.
- בורר חלון: רשימת חלונות פתוחים עם תצוגה מקדימה.
- **מסלול עיקרי לאייפד:** לכידת חלון השיקוף האלחוטי (Reflector או מקלט AirPlay המובנה של macOS). בניגוד ל-OBS, הלכידה כאן היא ScreenCaptureKit ישיר של החלון — נקייה וללא בעיות ההיפוך/איכות.
- **מקור "אייפד בכבל" (משני, אופציונלי):** אייפד המחובר בכבל מופיע ב-macOS כהתקן וידאו (המנגנון של QuickTime) וניתן לבחירה כמקור ישיר. יתרון: השהיה נמוכה יותר. באפליקציה הוא מופיע פשוט כעוד מקור בבורר — בלי חלון שמשתלט על המסך.
- החלון הנלכד ממולא לפריים (Fit עם רקע / Fill / בחירת אזור מתוך החלון — לבחירה בהגדרות).

### סצנה 3 — דפדפן מובנה + עיגול צף
- וידאו: WKWebView מובנה בחלון האפליקציה, מרונדר ישירות לסצנה, ומעליו עיגול מצלמה צף.
- שורת כתובת + היסטוריה בסיסית (אחורה/קדימה/רענון).
- שליטה מלאה בתוכן: גלילה, קליקים, הפעלת וידאו, מעבר שקפים.
- כפתורי "שקף קודם/הבא" גדולים + קיצורי חצים (שולחים חץ ימינה/שמאלה ל-WebView — עובד עם Google Slides, Canva, PowerPoint Online). זמינים גם **בפאנל הצף** (סעיף 8) — שליטה במצגת גם כשזום במסך מלא.
- מגבלה ידועה: אתרי DRM כבד לא יעבדו; אתר בעייתי → גיבוי בסצנה 2 (פתיחה בכרום ולכידת החלון).

---

## 3. העיגול הצף (סצנות 2–3)

- **צורות:** עיגול / מלבן מעוגל פינות (מתג בין השתיים).
- **שליטה ישירה:** גרירה על תצוגת ה-Preview לשינוי מיקום, ידיות/pinch לשינוי גודל — כולל תוך כדי שידור חי.
- **נעילה:** כפתור מנעול. במצב נעול — אי אפשר להזיז/לשנות גודל עד שחרור.
- **הסתרה זמנית:** כפתור עין (וקיצור מקלדת) שמעלים את העיגול; התוכן ממשיך להשתדר.
- מיקום וגודל נשמרים פר-סצנה בין הפעלות.

---

## 3ב. שמירת מצב בסגירה (Persistence)

בסגירת האפליקציה נשמר המצב המלא, ובפתיחה הבאה הכל חוזר בדיוק כפי שהיה:
- מיקום וגודל העיגול הצף (פר-סצנה) + צורה (עיגול/מלבן) + מצב נעילה
- המצלמה הנבחרת + מצב ה-Flip שלה
- רקע מצב ההמתנה
- החלון האחרון ששותף בסצנה 2 (זיהוי לפי אפליקציה + כותרת; אם החלון לא קיים — בורר החלונות נפתח)
- הקישור האחרון בסצנה 3 (הדפדפן נטען אליו אוטומטית)
- הסצנה האחרונה שהייתה פעילה, מיקום הפאנל הצף, וכל ההגדרות

---

## 4. ניהול מצלמה

- **בורר מצלמות:** תפריט של כל המצלמות הזמינות (מובנית / Continuity / USB), מתחלף בזמן אמת.
- **Flip ידני:** כפתור שיקוף אופקי, נשמר פר-מצלמה. (פתרון לבעיית ההיפוך של OBS.)
- זיהוי חיבור/ניתוק מצלמה תוך כדי עבודה בלי קריסה — מעבר אוטומטי למצלמה זמינה + התראה.

---

## 5. מיקסר ערוצים (השתקות)

לכל ערוץ מתג עצמאי, נגיש תמיד:

| ערוץ | השפעה |
|---|---|
| וידאו רקע (חלון/דפדפן) | מסך שחור/לוגו במקומו |
| וידאו עיגול (מצלמה) | העיגול נעלם |
| אודיו מיקרופון | מושתק בהקלטה |
| אודיו מערכת | מושתק בהקלטה |

הערה: השתקת מיקרופון בזום נעשית בזום עצמו (האפליקציה לא מזרימה אודיו לזום). ההשתקות כאן חלות על ההקלטה.

---

## 6. שידור (מצלמה וירטואלית)

- **טכנולוגיה:** CoreMediaIO Camera Extension (ה-API הרשמי).
- שם המצלמה בזום: שם האפליקציה.
- רזולוציה: 1920×1080 @ 30fps (המקסימום המעשי שזום צורך).
- **מצב "המתנה":** מציג רקע סטטי במקום הסצנה — **תמונה שהמשתמש מעלה בהגדרות** (לוגו, שקף "תיכף מתחילים" וכד'). בלי תמונה מועלית — מסך ברירת מחדל נקי. מופעל מכפתור "המתנה" בפס הכפתורים, וגם מוצג אוטומטית כשאין סצנה פעילה.
- אינדיקטור ברור בממשק: "משדר" (כשאפליקציה כלשהי צורכת את המצלמה).

## 7. הקלטה מקומית

- רצה במקביל לשידור, מאותה סצנה מרונדרת.
- **וידאו:** עד רזולוציית הסצנה המלאה (עד 4K אם המקור מאפשר), H.264/HEVC, קובץ MP4.
- **אודיו:** מיקרופון + סאונד מערכת (ScreenCaptureKit audio capture).
  - ברירת מחדל: ערוץ ממוקסס אחד (מוכן לשיתוף בכל פלטפורמה).
  - הגדרה: "הקלט ערוצים נפרדים" — שני ערוצי אודיו לעריכה.
- כפתור הקלטה + טיימר רץ + אינדיקטור אדום.
- שמירה אוטומטית לתיקייה נבחרת (ברירת מחדל: **הורדות/Downloads**, ניתן לבחור כל תיקייה אחרת בהגדרות), שם קובץ עם תאריך ושעה.
- עצירת חירום בטוחה — הקובץ תקין גם אם האפליקציה קורסת (fragmented MP4 / recovery).

---

## 8. שליטה ותפעול

### קיצורי מקלדת גלובליים (עובדים גם כשזום בחזית)
ברירות המחדל (⌃⌥ + מקש) נבחרו כי כמעט אף אפליקציה לא תופסת אותן; כל קיצור ניתן לשינוי במסך ההגדרות.
| פעולה | קיצור (ברירת מחדל, ניתן לשינוי) |
|---|---|
| מעבר לסצנה 1/2/3 | ⌃⌥1 / ⌃⌥2 / ⌃⌥3 |
| הסתר/הצג עיגול | ⌃⌥H |
| התחל/עצור הקלטה | ⌃⌥R |
| שידור הפעל/כבה | ⌃⌥B |
| מצב המתנה | ⌃⌥S |
| השתקת מיקרופון (בהקלטה) | ⌃⌥M |

### פס כפתורים קבוע (הממשק הראשי — פרוס בכל הסצנות)
פס עליון קבוע בחלון האפליקציה. כפתורי מצב נראים "לחוצים" כשהם פעילים:

| כפתור | התנהגות |
|---|---|
| **שידור** | מפעיל/מכבה את המצלמה הווירטואלית. נראה לחוץ + מואר כשמשדר. |
| **הקלט / עצור** | טוגל לפי מצב. נראה לחוץ + אינדיקטור אדום וטיימר כשמקליט. |
| **המתנה** | מציג את רקע ההמתנה במקום הסצנה (השידור נמשך עם התמונה הסטטית). |
| **סצנה 1 — מצלמה** | קבוצת שלושה כפתורים; הפעיל מסומן לחוץ, לחיצה מחליפה סצנה. |
| **סצנה 2 — שיתוף** | |
| **סצנה 3 — קישור** | |
| **השתק אודיו** | השתקת ערוצי האודיו בהקלטה (לחיצה ארוכה/חץ: פר-ערוץ, סעיף 5). |
| **הסתר וידאו** | מסתיר את העיגול הצף (בסצנה 1: מסתיר את המצלמה). |

### פאנל שליטה צף (Always on Top)
- **גרסה קומפקטית וצפה של פס הכפתורים**, שמרחפת מעל כל האפליקציות — כולל זום במסך מלא (NSPanel ברמת floating + fullScreenAuxiliary).
- מכיל: שידור, הקלט/עצור, המתנה, שלוש הסצנות, השתק אודיו, הסתר וידאו — כולם עם חיווי "לחוץ".
- ניתן לגרירה לכל מקום במסך, לכיווץ לפס מינימלי, ולסגירה (הפעלה מחדש מהמנו בר).
- **בסצנה 3 מתווספים לפאנל כפתורי שליטה במצגת:** שקף קודם / שקף הבא (+ הפעל/השהה וידאו).
- **חשוב:** הפאנל לא נראה לצופים בזום — השידור מרונדר מהסצנה, לא מהמסך, כך שהפאנל מרחף רק אצלך.

### נוסף
- Preview חי של מה שמשודר (זה גם משטח הגרירה של העיגול).
- אייקון בשורת התפריטים (menu bar) — **משלים, לא מחליף**: גישה מהירה להחלפת סצנה, הקלטה, המתנה והסתרת עיגול כשזום בחזית והאפליקציה מאחור.

### מעברי סצנה
- מעבר חלק (crossfade קצר ~0.3ש') — לא קפיצה קשה בזום.

---

## 9. ממשק

- **שפה:** עברית מלאה, RTL.
- עיצוב נקי ומינימלי — "ההפך מ-OBS": רק מה שצריך, בלי מיקסרים וסצנות אינסופיות.
- מסך הגדרות אחד:
  - תיקיית שמירה (ברירת מחדל: הורדות)
  - פורמט אודיו (ממוקסס / ערוצים נפרדים)
  - רזולוציית הקלטה
  - קיצורי מקלדת (שינוי כל קיצור)
  - מילוי חלון נלכד: **Fill / Fit / בחירת אזור** (חיתוך אזור מתוך החלון — למשל רק משטח הציור באייפד, בלי סרגלי כלים)
  - העלאת רקע למצב המתנה

---

## 10. הרשאות ודרישות מערכת

- **הרשאות macOS:** מצלמה, מיקרופון, הקלטת מסך (Screen Recording).
- **Camera Extension:** דורש אישור המשתמש בהגדרות המערכת בהתקנה ראשונה (הליך חד-פעמי מודרך).
- **חתימה:** Developer ID + notarization (דורש חשבון Apple Developer, $99/שנה) — בלעדיו התוסף לא ייטען.
- macOS 14 ומעלה (Sonoma) — נדרש ל-ScreenCaptureKit audio מלא.

---

## 11. שלבי פיתוח מוצעים

**שלב 1 — שלד + סצנה 1:**
אפליקציה, בורר מצלמה + flip, מצלמה וירטואלית עובדת בזום, הקלטה בסיסית (וידאו + מיקרופון).

**שלב 2 — סצנה 2:**
לכידת חלון, עיגול צף עם גרירה/גודל/נעילה/הסתרה, סאונד מערכת בהקלטה, מיקסר ערוצים.

**שלב 3 — סצנה 3:**
דפדפן מובנה, כפתורי שקפים, קיצורים גלובליים, menu bar.

**שלב 4 — ליטוש:**
מעברים חלקים, שמירת מצב פר-סצנה, הגדרות, התאוששות מקריסה, עברית מלאה.

כל שלב מסתיים במוצר שמיש — כבר בסוף שלב 1 יש תחליף עובד ל-OBS לשיחה רגילה.

---

## 12. סיכונים ונקודות לבדיקה מוקדמת

1. **Camera Extension + חתימה** — החלק הבירוקרטי הכי רגיש. לבדוק ראשון (Proof of Concept בשלב 1).
2. **ביצועים** — רינדור סצנה + שידור + הקלטה 4K במקביל. ב-Apple Silicon צפוי להיות בסדר; לבדוק על המכונה בפועל.
3. **לכידת חלון שיקוף iPad** — לוודא שחלון ה-Mirroring נלכד תקין ב-ScreenCaptureKit (צפוי שכן).
4. **Google Slides ב-WKWebView** — בדיקת התחברות חשבון ותפקוד מעבר שקפים.
